# 电商智能排序系统 — 技术说明书

## 1. 项目概述

**电商智能排序系统** 是一个基于机器学习技术的商品搜索排序平台。系统通过 XGBoost 模型对 Elasticsearch 的召回结果进行智能重排序，综合考虑商品价格、点击率、购买转化率、用户行为特征及文本相关性（BM25）等多种因素，为用户提供个性化的搜索排序体验。

系统提供完整的离线训练与在线推理链路：
- **Python 模块**：负责模拟数据生成、特征工程、XGBoost 模型训练与导出、离线数据加载
- **Java 模块**：基于 Spring Boot 框架提供在线搜索排序 REST API，集成 XGBoost 原生模型进行实时预测

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                          用户 / 前端 (index.html)                     │
└──────────────────────────┬──────────────────────────────────────────┘
                           │  HTTP GET /api/search?query=&userId=
                           ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      Java Spring Boot 后端                           │
│                                                                      │
│  ┌──────────────┐    ┌──────────────────┐    ┌───────────────────┐   │
│  │ RankingController│──▶ SearchFeatureService│──▶ XGBoostRanking │   │
│  └──────────────┘    └──────┬───┬────────┘    │   Service        │   │
│                             │   │             └───────────────────┘   │
│                    ┌────────▼───▼────────┐                           │
│                    │  Redis  │  ES        │                           │
│                    │ (特征缓存)│ (商品索引) │                           │
│                    └─────────┴───────────┘                           │
└──────────────────────────────────────────────────────────────────────┘
                          ▲
                          │  (离线加载)
┌──────────────────────────────────────────────────────────────────────┐
│                      Python 数据处理流水线                            │
│                                                                      │
│  ① generate_mock_data.py ──▶ ② feature_engineering.py               │
│  ③ train_export_pmml.py  ──▶ ④ load_to_redis_es.py                  │
└──────────────────────────────────────────────────────────────────────┘
```

### 核心流程

1. **ES 召回**：根据用户查询词（query）从 Elasticsearch 中召回 Top 50 相关商品
2. **特征组装**：从 Redis 中获取商品特征（价格、CTR、购买率）和用户特征（近7天点击量），结合 ES 返回的 BM25 得分
3. **模型打分**：将组装好的特征向量输入 XGBoost 模型进行预测
4. **重排序**：根据模型预测分数对召回商品进行降序排列，返回排序后的结果

---

## 3. 技术栈

### 3.1 后端（Java）

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Spring Boot | 3.3.0 | 应用主框架 |
| JDK | Java | 17 | 运行环境 |
| 构建工具 | Maven | - | 项目构建 |
| 搜索引擎 | Elasticsearch | 7.17.9 | 商品全文检索 |
| ES 客户端 | elasticsearch-rest-high-level-client | 7.17.9 | ES 高级 REST 客户端 |
| 缓存 | Redis | - | 特征数据缓存 |
| 机器学习 | XGBoost4J | 2.0.3 | XGBoost 模型加载与推理 |
| 序列化 | Jackson | 2.15.2 | JSON 序列化 |
| 工具库 | Lombok | 1.18.36 | 简化代码 |

### 3.2 数据处理（Python）

| 组件 | 版本 | 说明 |
|------|------|------|
| pandas | 1.5.3 | 数据处理与分析 |
| numpy | 1.23.5 | 数值计算 |
| scikit-learn | 1.2.2 | 机器学习工具 |
| XGBoost | 1.7.5 | 梯度提升树模型 |
| sklearn2pmml | 0.99.7 | PMML 格式转换 |
| redis-py | 4.5.1 | Redis 客户端 |
| elasticsearch-py | 7.17.9 | ES 客户端 |

---

## 4. 模块详解

### 4.1 Python 数据流水线

#### 4.1.1 `1_generate_mock_data.py` — 模拟数据生成

- **功能**：生成模拟电商数据集，包括商品信息和用户交互日志
- **商品数据**：500 件商品，覆盖电子产品、服装、图书、家居、食品 5 个类目，随机价格区间 10~1000 元
- **交互数据**：5000 条用户行为日志，包含浏览（view, 70%）、加购（cart, 20%）、购买（purchase, 10%）三种事件类型
- **输出**：`data/products.csv`、`data/interactions.csv`

#### 4.1.2 `2_feature_engineering.py` — 特征工程

- **功能**：基于原始交互日志和商品信息进行特征提取与样本构造
- **统计特征**：
  - `ctr`：商品点击率（平滑处理：点击数 / (点击数 + 10)）
  - `purchase_rate`：商品购买转化率（平滑处理：购买数 / (点击数 + 10)）
- **训练样本**：以购买记录为正样本（label=1），随机采样未购买商品为负样本（label=0）
- **特征列**（共 5 个）：
  - `price`：商品价格
  - `ctr`：商品点击率
  - `purchase_rate`：商品购买转化率
  - `user_click_7d`：用户近7天点击次数（模拟）
  - `bm25_score`：ES 文本相关性得分（模拟）
- **输出**：`data/feature_matrix.csv`

#### 4.1.3 `3_train_export_pmml.py` — 模型训练

- **功能**：训练 XGBoost 二分类模型并导出
- **模型参数**：
  - `max_depth=3`：树的最大深度
  - `learning_rate=0.1`：学习率
  - `n_estimators=50`：树的数量
  - `eval_metric=logloss`：评估指标
- **模型输出**：`java/src/main/resources/xgboost_model.json`（XGBoost 原生 JSON 格式）
- **说明**：模型使用 `XGBClassifier`，训练完成后直接保存为 JSON 格式供 Java 端加载

#### 4.1.4 `4_load_to_redis_es.py` — 数据加载

- **功能**：将商品特征和用户特征加载到 Redis，将商品数据加载到 Elasticsearch
- **Redis 数据结构**：
  - `product:{product_id}` → `{"price": ..., "ctr": ..., "purchase_rate": ...}`
  - `user:{user_id}` → `{"user_click_7d": ...}`
- **ES 索引**：`products`
  - `product_id`：keyword 类型
  - `title`：text 类型（standard 分词器）
  - `category`：keyword 类型
  - `price`：float 类型

### 4.2 Java 在线服务

#### 4.2.1 `RankingApplication.java`

- Spring Boot 应用入口，使用 `@SpringBootApplication` 自动配置

#### 4.2.2 `EsConfig.java` — ES 配置

- 从 `application.properties` 读取 `es.host` 和 `es.port` 配置
- 提供 `RestHighLevelClient` Bean，连接 Elasticsearch 服务

#### 4.2.3 `RankingController.java` — 搜索 API 控制器

- **端点**：`GET /api/search`
- **参数**：
  - `query`（必填）：搜索关键词
  - `userId`（可选，默认 `user_1`）：用户 ID

#### 4.2.4 `SearchFeatureService.java` — 搜索排序服务

- **步骤**：
  1. 使用 ES 的 `match` 查询在 `products` 索引中召回 Top 50 商品
  2. 从 Redis 获取用户特征（`user:{userId}`, 默认 `user_click_7d=10`）
  3. 遍历每个召回商品，从 Redis 获取商品特征（`product:{productId}`）
  4. 组装 5 维特征向量 `[price, ctr, purchase_rate, user_click_7d, bm25_score]`
  5. 调用 `XGBoostRankingService` 进行模型预测
  6. 按预测分数降序排列并返回结果

#### 4.2.5 `XGBoostRankingService.java` — XGBoost 推理服务

- **模型加载**：使用 `@PostConstruct` 在应用启动时从 `classpath:xgboost_model.json` 加载 XGBoost 模型
- **预测流程**：
  1. 按固定特征顺序（`price, ctr, purchase_rate, user_click_7d, bm25_score`）提取特征值
  2. 构造 `DMatrix` 数据结构
  3. 调用 `Booster.predict()` 进行推理
  4. 根据输出维度判断模型类型（单值输出为回归模型，双值输出取正类概率）
- **健壮性处理**：特征缺失时默认填充 0 值

#### 4.2.6 `RankedProduct.java` — 返回结果模型

- 字段：`product_id`、`title`、`category`、`price`、`ranking_score`
- 包含智能类型转换的 Setter 方法，兼容各种数据来源的类型差异

### 4.3 前端界面

- **位置**：`java/src/main/resources/static/index.html`
- **技术**：原生 HTML + CSS + JavaScript（无前端框架依赖）
- **功能**：
  - 搜索表单（输入关键词和用户 ID）
  - 搜索统计面板（结果数量、最高分、平均分）
  - 商品列表卡片展示（含排名分数、价格、类目标签）
  - 加载状态提示与空结果处理

---

## 5. API 接口文档

### 5.1 搜索排序接口

```
GET /api/search?query={query}&userId={userId}
```

**请求参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| query | String | 是 | - | 搜索关键词 |
| userId | String | 否 | user_1 | 用户标识 |

**响应示例**

```json
[
  {
    "product_id": "prod_1",
    "title": "电子产品爆款商品1",
    "category": "电子产品",
    "price": 599.99,
    "ranking_score": 0.85
  },
  {
    "product_id": "prod_42",
    "title": "图书爆款商品42",
    "category": "图书",
    "price": 39.00,
    "ranking_score": 0.72
  }
]
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| product_id | String | 商品唯一标识 |
| title | String | 商品标题 |
| category | String | 商品类目 |
| price | Double | 商品价格 |
| ranking_score | Double | XGBoost 模型预测排序分数 |

---

## 6. 部署与运行

### 6.1 环境要求

| 组件 | 要求 |
|------|------|
| JDK | 17+ |
| Maven | 3.6+ |
| Python | 3.8+ |
| Redis | 任意可用版本 |
| Elasticsearch | 7.17.x |

### 6.2 快速启动

#### 步骤 1：安装 Python 依赖

```bash
cd python
pip install -r requirements.txt
```

#### 步骤 2：运行 Python 数据流水线

```bash
# (可选) 生成模拟数据
python 1_generate_mock_data.py

# (可选) 特征工程
python 2_feature_engineering.py

# (可选) 训练模型
python 3_train_export_pmml.py

# 加载数据到 Redis 和 ES
python 4_load_to_redis_es.py
```

#### 步骤 3：启动 Java 后端

```bash
cd java
mvn clean install -DskipTests
mvn spring-boot:run
```

#### 步骤 4：访问系统

浏览器打开 `http://localhost:8080` 即可使用搜索排序界面。

### 6.3 配置文件

`java/src/main/resources/application.properties`：

```properties
server.port=8080
spring.data.redis.host=localhost
spring.data.redis.port=6379
es.host=localhost
es.port=9200
```

---

## 7. 特征工程说明

模型使用 5 维特征向量进行排序预测：

| 特征 | 类型 | 来源 | 说明 |
|------|------|------|------|
| price | float | Redis / ES | 商品价格，反映价格对转化的影响 |
| ctr | float | Redis | 商品点击率，平滑处理防止稀疏 |
| purchase_rate | float | Redis | 商品购买转化率, 平滑处理防止稀疏 |
| user_click_7d | int | Redis | 用户近7天行为活跃度 |
| bm25_score | float | ES | 文本相关性得分，衡量 query 与 title 匹配度 |

> **注意**：特征顺序必须与训练时保持一致，Java 端通过 `FEATURE_COLUMNS` 列表严格对齐。

---

## 8. 项目结构

```
ecommerce-ranking-system/
├── data/                          # 数据文件目录
│   ├── products.csv               # 商品数据
│   ├── interactions.csv           # 用户交互日志
│   └── feature_matrix.csv         # 特征矩阵（训练数据）
│
├── python/                        # Python 数据处理模块
│   ├── 1_generate_mock_data.py    # 模拟数据生成
│   ├── 2_feature_engineering.py   # 特征工程
│   ├── 3_train_export_pmml.py     # 模型训练与导出
│   ├── 4_load_to_redis_es.py      # 数据加载到 Redis/ES
│   └── requirements.txt           # Python 依赖清单
│
├── java/                          # Java 后端服务
│   ├── pom.xml                    # Maven 构建配置
│   └── src/
│       └── main/
│           ├── java/com/example/ranking/
│           │   ├── RankingApplication.java          # 应用入口
│           │   ├── config/EsConfig.java              # ES 配置
│           │   ├── controller/RankingController.java # API 控制器
│           │   ├── model/RankedProduct.java          # 结果模型
│           │   └── service/
│           │       ├── SearchFeatureService.java     # 搜索排序服务
│           │       └── XGBoostRankingService.java    # XGBoost 推理
│           └── resources/
│               ├── static/index.html                 # 前端页面
│               ├── application.properties            # 应用配置
│               └── xgboost_model.json                # XGBoost 模型
│
└── 技术说明书.md                   # 本文件
```

---

## 9. 扩展与自定义

### 9.1 添加新特征

1. **Python 侧**：在 `2_feature_engineering.py` 中 `feature_cols` 列表添加新特征列
2. **训练侧**：重新运行 `3_train_export_pmml.py` 训练模型
3. **数据加载**：在 `4_load_to_redis_es.py` 中将新特征写入 Redis
4. **Java 侧**：在 `XGBoostRankingService.java` 的 `FEATURE_COLUMNS` 列表中添加对应特征名，并在 `SearchFeatureService.java` 中从 Redis 获取该特征

### 9.2 切换排序模型

- 当前使用 XGBoost 原生 JSON 格式模型，通过 XGBoost4J 加载
- 系统同时保留 PMML 格式支持（`search_ranking.pmml`），可切换为 JPMML 方式加载
- 如需使用深度学习模型，可在 `XGBoostRankingService.java` 基础上扩展新 Service

---

## 10. 注意事项

1. **特征一致性**：Java 在线推理时的特征顺序和数据类型必须与 Python 训练时完全一致，否则会导致预测结果异常
2. **Redis 数据预热**：首次启动应用前，必须先运行 `4_load_to_redis_es.py` 确保 Redis 和 ES 中有数据
3. **Elasticsearch 版本**：使用 7.17.x 版本，ES 8.x 需要使用新的 Java Client API
4. **模拟数据说明**：当前数据为随机生成的模拟数据，`bm25_score` 和 `user_click_7d` 为模拟值，实际生产环境需接入真实数据源
